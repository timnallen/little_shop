<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "Placeholder Site Name", welcome_index_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><%= link_to 'Items', items_path, class: "nav-link" %></li>
      <li class="nav-item"><%= link_to 'Merchants', users_path, class: "nav-link" %></li>
      <% if current_user %>
      <%= render partial: 'nav/registered' if current_user.registered? %>
      <%= render partial: 'nav/merchant' if current_user.merchant? %>
      <%= render partial: 'nav/admin' if current_user.admin? %>
      <li class="nav-item">Logged in as: <%= current_user.name %></li>
      <li class="nav-item"><%= link_to 'Logout', logout_path, class: "nav-link" %></li>
      <% else %>
      <%= render partial: "nav/visitor" %>
      <% end %>
    </ul>
  </div>
</nav>
