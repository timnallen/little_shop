<%= @orders.each do |order| %>
  <%= link_to order.id, admin_order_path(order) %>
  <%= order.created_at %>
  <%= order.updated_at %>
  <%= order.quantity_of_items %>
  <%= order.grand_total %>
<% end %>
