<%= @orders.each do |order| %>
  <%= link_to order.id, profile_order_path(order) %>
  <%= order.created_at %>
  <%= order.updated_at %>
  <%= order.quantity_of_items %>
  <%= order.grand_total %>
  <%= order.status %>
<% end %>
