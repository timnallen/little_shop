<% if !@item.errors.empty? %>
<div class="alert alert-danger">
  <ul id="errors">
    <% @item.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
